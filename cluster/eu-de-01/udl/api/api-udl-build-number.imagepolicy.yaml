---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: api-udl-build-number
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: api-udl
  filterTags:
    pattern: '^build-(?P<build_number>\d+)$'
    extract: '$build_number'
  policy:
    numerical:
      order: asc
