apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: update-controller-test-two
  namespace: udl
spec:
  chartRef:
    kind: OCIRepository
    name: update-controller-test-two
    namespace: default
  interval: 10m
  targetNamespace: udl
  values:
    config:
      podSelector: "update=test-two"
      namespace: "udl"
    namespace:
      create: false
      name: udl
    persistence:
      enabled: true
      type: hostPath
      hostPath:
        path: "/mnt/test-two"
        type: DirectoryOrCreate
    securityContext:
      runAsUser: 1000
      runAsGroup: 1000
      fsGroup: 1000
